<h2>Edit Hotel details</h2>
<%= form_with model: [@partner, @hotel] do |f| %>
<div class="field">
	<%= f.label :hotel_name %>
	<%= f.text_field :hotel_name %>
</div>
<div class="field">
	<%= f.label :city %>
	<%= f.text_field :city %>
</div>
<div> <%= f.submit %></div>
<% end %>