<h2>Details of Tour <%= @tour.destination_name %> organized by <%= @tour.organizer.name %></h2>

<p><%= link_to 'Click here for Organizer Details',organizer_path(@tour.organizer) %></p>
<p>Images of Tour:
<% @tour.images.each do |image| %>
   <%= image_tag url_for(image) %>
<% end %>
</p>
<p>Destination Name:<%= @tour.destination_name %></p>
<p>Pick up / Dropping Point: <%= @tour.pickup_and_dropping_point %></p>
<p>Pick up Date:<%= @tour.pickup_date %></p>
<p>Dropping Date: <%= @tour.dropping_date %></p>
<p>Inclusions: <%= @tour.inclusions %></p>
<p>Amount: <%= @tour.amount %></p>

<button><%= link_to 'Back', travellers_path %></button>
<button><%= link_to'Booked Tour', new_traveller_booking_path(@traveller, tour_id:@tour.id)%></button>